# 校园二手物品交易平台详细设计文档

## 1. 引言

### 1.1 文档目的
本文档详细描述校园二手物品交易平台的技术实现细节，包括各微服务模块的具体设计、类图、接口定义、数据库表结构等，为开发人员提供明确的开发指导。

### 1.2 术语定义
- **微服务**：一种软件架构风格，将应用程序构建为一系列松耦合的服务
- **Nacos**：阿里巴巴开源的服务注册发现和配置管理平台
- **Sentinel**：流量控制、熔断降级的服务保护框架
- **Seata**：分布式事务解决方案
- **MyBatis-Plus**：MyBatis的增强工具，简化开发
- **JWT**：JSON Web Token，用于安全地传输信息的标准
- **WebSocket**：提供全双工通信通道的网络协议

## 2. 用户服务 (User-Service) 详细设计

### 2.1 类设计

```
┌───────────────┐     ┌─────────────────┐     ┌──────────────────┐
│    User       │     │   UserAuth      │     │    Favorite      │
├───────────────┤     ├─────────────────┤     ├──────────────────┤
│ -id: Long     │     │ -id: Long       │     │ -id: Long        │
│ -username: String │ │ -userId: Long   │     │ -userId: Long    │
│ -nickname: String │ │ -password: String│    │ -productId: Long │
│ -avatar: String   │ │ -phone: String  │     │ -createTime: Date│
│ -gender: Integer  │ │ -email: String  │     └──────────────────┘
│ -college: String  │ │ -createTime: Date│
│ -grade: Integer   │ └─────────────────┘
│ -status: Integer  │
│ -createTime: Date │     ┌─────────────────────┐
└───────────────┘     │ NotificationSetting  │
                      ├─────────────────────┤
┌───────────────┐     │ -id: Long           │
│  UserController│    │ -userId: Long       │
├───────────────┤     │ -messageEnabled: Boolean│
│ +register()   │     │ -tradeEnabled: Boolean │
│ +login()      │     │ -systemEnabled: Boolean│
│ +getUserInfo()│     └─────────────────────┘
│ +updateInfo() │
└───────┬───────┘
        │
┌───────▼───────┐     ┌──────────────────┐
│   UserService  │     │    UserMapper    │
├───────────────┤     ├──────────────────┤
│ +register()   │     │ @Insert          │
│ +login()      │     │ @Select          │
│ +getUserInfo()│     │ @Update          │
│ +updateInfo() │     └──────────────────┘
└───────────────┘
```

### 2.2 接口设计

#### 2.2.1 用户注册接口
- **URL**: `/api/v1/users/register`
- **方法**: `POST`
- **请求体**:
```json
{
  "username": "student123",
  "password": "password123",
  "phone": "13800138000",
  "nickname": "小明",
  "college": "计算机学院",
  "grade": 2021
}
```
- **响应体**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": 1,
    "username": "student123",
    "token": "eyJhbGciOiJIUzI1NiJ9..."
  }
}
```

#### 2.2.2 用户登录接口
- **URL**: `/api/v1/users/login`
- **方法**: `POST`
- **请求体**:
```json
{
  "username": "student123",
  "password": "password123"
}
```
- **响应体**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": 1,
    "username": "student123",
    "token": "eyJhbGciOiJIUzI1NiJ9..."
  }
}
```

#### 2.2.3 收藏管理接口
- **URL**: `/api/v1/users/favorites`
- **方法**: `GET/POST/DELETE`
- **请求体** (POST):
```json
{
  "productId": 1001
}
```

### 2.3 配置项
- `jwt.secret`: JWT签名密钥
- `user.password.salt`: 密码加盐值
- `user.login.max.attempts`: 最大登录尝试次数
- `user.lock.timeout`: 账户锁定时间(分钟)

## 3. 商品服务 (Product-Service) 详细设计

### 3.1 类设计

```
┌───────────────┐     ┌─────────────────┐     ┌──────────────────┐
│    Product    │     │  ProductImage   │     │     Category     │
├───────────────┤     ├─────────────────┤     ├──────────────────┤
│ -id: Long     │     │ -id: Long       │     │ -id: Long        │
│ -title: String│     │ -productId: Long│     │ -name: String    │
│ -description: String││ -url: String    │     │ -parentId: Long  │
│ -price: BigDecimal │ │ -sort: Integer  │     │ -icon: String    │
│ -userId: Long  │     └─────────────────┘     │ -sort: Integer   │
│ -categoryId: Long│                           │ -status: Integer │
│ -status: Integer │                           └──────────────────┘
│ -viewCount: Integer│
│ -createTime: Date  │
└───────────────┘     ┌──────────────────┐
                      │    ProductTag    │
┌───────────────┐     ├──────────────────┤
│ ProductController │ │ -id: Long        │
├───────────────┤     │ -name: String    │
│ +create()     │     └──────────────────┘
│ +update()     │
│ +delete()     │     ┌──────────────────┐
│ +getById()    │     │  ProductMapper   │
│ +list()       │     ├──────────────────┤
└───────┬───────┘     │ @Insert          │
        │             │ @Select          │
┌───────▼───────┐     │ @Update          │
│  ProductService│     │ @Delete          │
├───────────────┤     └──────────────────┘
│ +create()     │
│ +update()     │
│ +delete()     │
│ +getById()    │
│ +list()       │
└───────────────┘
```

### 3.2 接口设计

#### 3.2.1 发布商品接口
- **URL**: `/api/v1/products`
- **方法**: `POST`
- **请求体**:
```json
{
  "title": "全新MacBook Pro 2023",
  "description": "全新未拆封，支持验货",
  "price": 9999.00,
  "categoryId": 1,
  "images": ["url1", "url2"],
  "tags": ["全新", "电子产品"]
}
```

#### 3.2.2 商品列表接口
- **URL**: `/api/v1/products?categoryId=1&minPrice=1000&maxPrice=5000&page=1&size=10`
- **方法**: `GET`
- **响应体**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "list": [
      {
        "id": 1,
        "title": "MacBook Pro",
        "price": 9999.00,
        "mainImage": "url1",
        "userId": 1,
        "createTime": "2023-01-01 10:00:00"
      }
    ]
  }
}
```

### 3.3 配置项
- `product.image.max.size`: 商品图片最大大小(MB)
- `product.image.max.count`: 单个商品最大图片数量
- `product.page.size`: 默认分页大小
- `product.search.timeout`: 搜索超时时间(ms)

## 4. 订单服务 (Order-Service) 详细设计

### 4.1 类设计

```
┌───────────────┐     ┌─────────────────┐     ┌──────────────────┐
│     Order     │     │   OrderItem     │     │      Review      │
├───────────────┤     ├─────────────────┤     ├──────────────────┤
│ -id: Long     │     │ -id: Long       │     │ -id: Long        │
│ -orderNo: String│   │ -orderId: Long  │     │ -orderId: Long   │
│ -userId: Long  │     │ -productId: Long│     │ -productId: Long │
│ -sellerId: Long│    │ -productName: String│   │ -userId: Long    │
│ -totalPrice: BigDecimal │ -price: BigDecimal│ │ -rating: Integer │
│ -status: Integer │  │ -quantity: Integer│   │ -content: String │
│ -tradeType: Integer│└─────────────────┘     │ -images: String  │
│ -createTime: Date  │                        │ -createTime: Date│
└───────────────┘                             └──────────────────┘

┌───────────────┐     ┌──────────────────┐
│OrderController│     │    OrderMapper   │
├───────────────┤     ├──────────────────┤
│ +create()     │     │ @Insert          │
│ +updateStatus()│    │ @Select          │
│ +getById()    │     │ @Update          │
│ +list()       │     └──────────────────┘
└───────┬───────┘
        │
┌───────▼───────┐     ┌──────────────────┐
│  OrderService  │     │   ReviewMapper   │
├───────────────┤     ├──────────────────┤
│ +create()     │     │ @Insert          │
│ +updateStatus()│    │ @Select          │
│ +getById()    │     └──────────────────┘
│ +list()       │
└───────────────┘
```

### 4.2 接口设计

#### 4.2.1 创建订单接口
- **URL**: `/api/v1/orders`
- **方法**: `POST`
- **请求体**:
```json
{
  "productId": 1,
  "price": 9999.00,
  "tradeType": 1, // 1: 线下交易, 2: 快递
  "remark": "希望尽快交易"
}
```

#### 4.2.2 更新订单状态接口
- **URL**: `/api/v1/orders/{id}/status`
- **方法**: `PUT`
- **请求体**:
```json
{
  "status": 3 // 订单状态码
}
```

#### 4.2.3 创建评价接口
- **URL**: `/api/v1/orders/{id}/review`
- **方法**: `POST`
- **请求体**:
```json
{
  "rating": 5,
  "content": "商品质量很好，卖家服务态度也很棒！",
  "images": ["url1"]
}
```

### 4.3 配置项
- `order.number.prefix`: 订单号前缀
- `order.timeout.minutes`: 订单超时时间(分钟)
- `review.rating.max`: 最大评分值
- `review.image.max.count`: 评价图片最大数量

## 5. 聊天服务 (Chat-Service) 详细设计

### 5.1 类设计

```
┌───────────────┐     ┌─────────────────┐     ┌──────────────────┐
│ ChatSession   │     │     Message     │     │   MessageType    │
├───────────────┤     ├─────────────────┤     ├──────────────────┤
│ -id: Long     │     │ -id: Long       │     │ 文本消息         │
│ -userId1: Long│     │ -sessionId: Long│     │ 图片消息         │
│ -userId2: Long│     │ -senderId: Long │     │ 语音消息         │
│ -lastMessage: String││ -receiverId: Long│     │ 文件消息         │
│ -lastTime: Date│    │ -content: String│     └──────────────────┘
│ -unreadCount: Integer │ -type: Integer │
└───────────────┘     │ -status: Integer│
                      │ -createTime: Date│
┌───────────────┐     └─────────────────┘
│ChatController │     ┌──────────────────┐
├───────────────┤     │  MessageMapper   │
│ +getSessions()│     ├──────────────────┤
│ +getHistory() │     │ @Insert          │
└───────┬───────┘     │ @Select          │
        │             └──────────────────┘
┌───────▼───────┐     ┌──────────────────┐
│ ChatWebSocketHandler││ ChatService     │
├───────────────┤     ├──────────────────┤
│ +handleMessage()│   │ +getSessions()  │
│ +connect()    │     │ +getHistory()   │
│ +disconnect() │     │ +saveMessage()  │
└───────────────┘     └──────────────────┘
```

### 5.2 接口设计

#### 5.2.1 WebSocket连接
- **URL**: `/ws/chat/{userId}`
- **连接参数**: JWT Token
- **消息格式**:
```json
// 发送消息
{
  "type": "send",
  "receiverId": 2,
  "content": "你好，这个商品还在吗？",
  "messageType": 1
}

// 接收消息
{
  "type": "receive",
  "sessionId": 1,
  "senderId": 1,
  "content": "你好，这个商品还在吗？",
  "createTime": "2023-01-01 10:00:00"
}
```

#### 5.2.2 获取聊天记录接口
- **URL**: `/api/v1/chat/history/{sessionId}?page=1&size=50`
- **方法**: `GET`

### 5.3 配置项
- `websocket.max.frame.size`: 最大消息帧大小
- `websocket.message.cache.size`: 消息缓存大小
- `message.store.days`: 消息存储天数
- `websocket.heartbeat.interval`: 心跳间隔(秒)

## 6. 搜索服务 (Search-Service) 详细设计

### 6.1 类设计

```
┌───────────────┐     ┌─────────────────┐     ┌──────────────────┐
│ SearchHistory │     │    HotWord      │     │ ProductDocument  │
├───────────────┤     ├─────────────────┤     ├──────────────────┤
│ -id: Long     │     │ -id: Long       │     │ -id: Long        │
│ -userId: Long │     │ -keyword: String│     │ -title: String   │
│ -keyword: String│   │ -count: Integer │     │ -description: String│
│ -createTime: Date│  │ -updateTime: Date│    │ -price: Double   │
└───────────────┘     └─────────────────┘     │ -categoryId: Long │
                                              │ -userId: Long    │
┌───────────────┐     ┌──────────────────┐     │ -status: Integer │
│SearchController│    │SearchHistoryMapper│    │ -viewCount: Integer│
├───────────────┤     ├──────────────────┤     └──────────────────┘
│ +search()     │     │ @Insert          │
│ +getHotWords()│     │ @Select          │
│ +getHistory() │     │ @Delete          │
└───────┬───────┘     └──────────────────┘
        │
┌───────▼───────┐     ┌──────────────────┐
│  SearchService │     │  ElasticsearchClient │
├───────────────┤     ├──────────────────┤
│ +search()     │     │ +search()       │
│ +getHotWords()│     │ +index()        │
│ +getHistory() │     │ +delete()       │
└───────────────┘     └──────────────────┘
```

### 6.2 接口设计

#### 6.2.1 商品搜索接口
- **URL**: `/api/v1/search/products?keyword=macbook&page=1&size=10&sort=price_asc`
- **方法**: `GET`
- **响应体**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 20,
    "list": [
      {
        "id": 1,
        "title": "MacBook Pro 2023",
        "description": "描述...",
        "price": 9999.00,
        "mainImage": "url1",
        "userId": 1
      }
    ],
    "suggestions": ["macbook air", "macbook pro 14"]
  }
}
```

#### 6.2.2 热门搜索词接口
- **URL**: `/api/v1/search/hotwords?limit=10`
- **方法**: `GET`

### 6.3 配置项
- `elasticsearch.hosts`: ES集群地址
- `elasticsearch.index.product`: 商品索引名称
- `hotword.count.limit`: 热门词数量限制
- `search.history.max.count`: 单用户搜索历史最大数量
- `search.result.page.size`: 搜索结果默认分页大小

## 7. 公共组件设计

### 7.1 统一响应格式

```java
public class ApiResponse<T> {
    private int code;
    private String message;
    private T data;
    
    // 构造方法、getter、setter...
    
    public static <T> ApiResponse<T> success(T data) {
        return new ApiResponse<>(200, "success", data);
    }
    
    public static <T> ApiResponse<T> error(int code, String message) {
        return new ApiResponse<>(code, message, null);
    }
}
```

### 7.2 统一异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(Exception.class)
    public ApiResponse<?> handleException(Exception e) {
        // 异常处理逻辑
        return ApiResponse.error(500, "服务器内部错误");
    }
    
    @ExceptionHandler(BusinessException.class)
    public ApiResponse<?> handleBusinessException(BusinessException e) {
        return ApiResponse.error(e.getCode(), e.getMessage());
    }
}
```

### 7.3 JWT工具类

```java
@Component
public class JwtUtils {
    
    @Value("${jwt.secret}")
    private String secret;
    
    @Value("${jwt.expire}")
    private long expire;
    
    public String generateToken(Long userId) {
        // 生成JWT token
    }
    
    public Claims parseToken(String token) {
        // 解析JWT token
    }
    
    public boolean validateToken(String token) {
        // 验证token
    }
}
```

### 7.4 日志切面

```java
@Aspect
@Component
public class LogAspect {
    
    private static final Logger logger = LoggerFactory.getLogger(LogAspect.class);
    
    @Around("execution(* com.secondhand.*.controller.*.*(..))")
    public Object logAround(ProceedingJoinPoint joinPoint) throws Throwable {
        // 记录请求参数
        // 执行方法
        // 记录响应结果
        // 记录执行时间
    }
}
```

## 8. 数据库交互设计

### 8.1 MyBatis-Plus 配置

```java
@Configuration
@MapperScan("com.secondhand.*.mapper")
public class MyBatisPlusConfig {
    
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        // 添加分页插件
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
        return interceptor;
    }
}
```

### 8.2 通用Mapper设计

```java
public interface BaseMapper<T> extends com.baomidou.mybatisplus.core.mapper.BaseMapper<T> {
    // 通用查询方法
    List<T> selectByCondition(T entity, Page<T> page);
    
    // 通用更新方法
    boolean updateByCondition(T entity, T condition);
}
```

### 8.3 XML映射文件示例 (复杂查询)

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secondhand.product.mapper.ProductMapper">
    
    <select id="selectProductWithFilters" resultType="com.secondhand.product.pojo.Product">
        SELECT p.* 
        FROM t_product p
        LEFT JOIN t_category c ON p.category_id = c.id
        WHERE 1=1
        <if test="keyword != null and keyword != ''">
            AND (p.title LIKE CONCAT('%', #{keyword}, '%') OR p.description LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="categoryId != null">
            AND p.category_id = #{categoryId}
        </if>
        <if test="minPrice != null">
            AND p.price >= #{minPrice}
        </if>
        <if test="maxPrice != null">
            AND p.price <= #{maxPrice}
        </if>
        <if test="status != null">
            AND p.status = #{status}
        </if>
        ORDER BY p.create_time DESC
    </select>
</mapper>
```

## 9. 服务间通信设计

### 9.1 OpenFeign客户端示例

```java
@FeignClient(name = "user-service")
public interface UserServiceClient {
    
    @GetMapping("/api/v1/users/{id}")
    ApiResponse<UserInfo> getUserInfo(@PathVariable("id") Long userId);
    
    @PostMapping("/api/v1/users/{id}/notify")
    ApiResponse<Boolean> sendNotification(@PathVariable("id") Long userId, @RequestBody NotificationRequest request);
}
```

### 9.2 服务调用示例

```java
@Service
public class OrderServiceImpl implements OrderService {
    
    @Autowired
    private UserServiceClient userServiceClient;
    
    @Autowired
    private ProductServiceClient productServiceClient;
    
    @Override
    @Transactional
    public Order createOrder(OrderCreateRequest request) {
        // 1. 调用用户服务验证用户信息
        ApiResponse<UserInfo> userResponse = userServiceClient.getUserInfo(request.getUserId());
        
        // 2. 调用商品服务验证商品信息和状态
        ApiResponse<ProductInfo> productResponse = productServiceClient.getProductInfo(request.getProductId());
        
        // 3. 创建订单
        Order order = new Order();
        // 设置订单信息
        orderMapper.insert(order);
        
        // 4. 发送消息通知卖家
        NotificationRequest notifyRequest = new NotificationRequest();
        notifyRequest.setType("order_created");
        notifyRequest.setContent("您有一个新订单");
        userServiceClient.sendNotification(productResponse.getData().getUserId(), notifyRequest);
        
        return order;
    }
}
```

## 10. 安全设计

### 10.1 JWT认证过滤器

```java
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    
    @Autowired
    private JwtUtils jwtUtils;
    
    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain) throws ServletException, IOException {
        // 获取token
        String token = request.getHeader("Authorization");
        if (token != null && token.startsWith("Bearer ")) {
            token = token.substring(7);
            
            if (jwtUtils.validateToken(token)) {
                Claims claims = jwtUtils.parseToken(token);
                Long userId = Long.valueOf(claims.getSubject());
                
                // 设置用户信息到上下文
                SecurityContext context = SecurityContextHolder.createEmptyContext();
                UsernamePasswordAuthenticationToken authToken = new UsernamePasswordAuthenticationToken(userId, null, Collections.emptyList());
                context.setAuthentication(authToken);
                SecurityContextHolder.setContext(context);
            }
        }
        
        filterChain.doFilter(request, response);
    }
}
```

### 10.2 权限控制示例

```java
@RestController
@RequestMapping("/api/v1/users")
public class UserController {
    
    @GetMapping("/info")
    @PreAuthorize("@authService.isAuthenticated()")
    public ApiResponse<UserInfo> getUserInfo() {
        Long userId = SecurityContextHolder.getContext().getAuthentication().getPrincipal();
        // 获取用户信息
    }
    
    @PutMapping("/update")
    @PreAuthorize("@authService.isAuthenticated()")
    public ApiResponse<Boolean> updateUserInfo(@RequestBody UserUpdateRequest request) {
        // 更新用户信息
    }
}
```

## 11. 部署与集成方案

### 11.1 Dockerfile示例

```dockerfile
FROM openjdk:11-jre-slim

WORKDIR /app

COPY target/*.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]
```

### 11.2 Docker Compose配置

```yaml
version: '3'
services:
  nacos:
    image: nacos/nacos-server:v2.0.3
    ports:
      - "8848:8848"
    environment:
      - MODE=standalone
      
  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=secondhand
    volumes:
      - mysql-data:/var/lib/mysql
      
  redis:
    image: redis:6.2
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
      
  user-service:
    build: ./user-service
    ports:
      - "8081:8080"
    depends_on:
      - nacos
      - mysql
    
  product-service:
    build: ./product-service
    ports:
      - "8082:8080"
    depends_on:
      - nacos
      - mysql
      
  # 其他服务配置...

volumes:
  mysql-data:
  redis-data:
```

### 11.3 Nginx配置

```nginx
user nginx;
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    sendfile on;
    keepalive_timeout 65;
    
    upstream gateway {
        server gateway:8080;
    }
    
    server {
        listen 80;
        server_name localhost;
        
        # 前端静态资源
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }
        
        # API请求转发
        location /api {
            proxy_pass http://gateway;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
        
        # WebSocket连接
        location /ws {
            proxy_pass http://gateway;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }
}
```

## 12. 监控与日志设计

### 12.1 日志配置 (Logback)

```xml
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
    
    <logger name="com.secondhand" level="DEBUG" />
</configuration>
```

### 12.2 健康检查端点

```java
@RestController
@RequestMapping("/actuator")
public class HealthController {
    
    @GetMapping("/health")
    public Map<String, Object> health() {
        Map<String, Object> response = new HashMap<>();
        response.put("status", "UP");
        response.put("timestamp", new Date());
        response.put("service", "secondhand-service");
        return response;
    }
    
    @GetMapping("/info")
    public Map<String, Object> info() {
        Map<String, Object> response = new HashMap<>();
        response.put("version", "1.0.0");
        response.put("name", "校园二手物品交易平台");
        return response;
    }
}
```

## 13. 附录

### 13.1 错误码定义

| 错误码 | 描述 |
|-------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 409 | 资源冲突 |
| 500 | 服务器内部错误 |
| 10001 | 用户不存在 |
| 10002 | 密码错误 |
| 10003 | 用户名已存在 |
| 20001 | 商品不存在 |
| 20002 | 商品已下架 |
| 30001 | 订单不存在 |
| 30002 | 订单状态错误 |
| 40001 | 消息发送失败 |
| 50001 | 搜索服务异常 |

### 13.2 数据字典

#### 13.2.1 用户状态
- 0: 禁用
- 1: 正常
- 2: 待审核

#### 13.2.2 商品状态
- 0: 下架
- 1: 在售
- 2: 已售
- 3: 审核中
- 4: 审核失败

#### 13.2.3 订单状态
- 0: 待付款
- 1: 待发货
- 2: 待收货
- 3: 待评价
- 4: 已完成
- 5: 已取消
- 6: 已退款

#### 13.2.4 交易类型
- 1: 线下交易
- 2: 快递配送
# 校园二手物品交易平台数据库设计文档

## 1. 数据库概述

### 1.1 数据库选型
本系统采用 MySQL 8.0 作为关系型数据库，主要考虑以下因素：
- 成熟稳定，社区活跃
- 良好的性能和扩展性
- 丰富的功能和工具生态
- 与 Spring Boot 等框架的良好集成

### 1.2 数据库设计原则
- **三范式原则**：确保数据的一致性和减少冗余
- **主键和外键约束**：维护数据完整性
- **索引优化**：提高查询性能
- **安全性考虑**：敏感数据加密存储
- **可扩展性**：预留字段和设计，适应未来需求变化

### 1.3 数据库字符集设置
- 数据库字符集：`utf8mb4`
- 排序规则：`utf8mb4_unicode_ci`
- 目的：支持更广泛的字符集，特别是emoji表情等特殊字符

## 2. 数据库表结构设计

### 2.1 用户相关表

#### 2.1.1 `t_user` - 用户基本信息表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 用户ID |
| `username` | `VARCHAR(50)` | `NOT NULL UNIQUE` | 用户名 |
| `nickname` | `VARCHAR(50)` | `NOT NULL` | 用户昵称 |
| `avatar` | `VARCHAR(255)` | `DEFAULT NULL` | 头像URL |
| `gender` | `TINYINT` | `DEFAULT 0` | 性别(0:未知,1:男,2:女) |
| `college` | `VARCHAR(100)` | `DEFAULT NULL` | 学院 |
| `grade` | `INT` | `DEFAULT NULL` | 年级 |
| `status` | `TINYINT` | `DEFAULT 1` | 状态(0:禁用,1:正常,2:待审核) |
| `last_login_time` | `DATETIME` | `DEFAULT NULL` | 最后登录时间 |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引设计**：
- 主键索引: `id`
- 唯一索引: `username`
- 普通索引: `status`

#### 2.1.2 `t_user_auth` - 用户认证信息表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 认证ID |
| `user_id` | `BIGINT` | `NOT NULL UNIQUE` | 用户ID，外键关联`t_user.id` |
| `password` | `VARCHAR(255)` | `NOT NULL` | 加密后的密码 |
| `phone` | `VARCHAR(20)` | `UNIQUE DEFAULT NULL` | 手机号 |
| `email` | `VARCHAR(100)` | `UNIQUE DEFAULT NULL` | 邮箱 |
| `login_attempts` | `INT` | `DEFAULT 0` | 登录失败次数 |
| `lock_time` | `DATETIME` | `DEFAULT NULL` | 锁定时间 |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引设计**：
- 主键索引: `id`
- 外键索引: `user_id`
- 唯一索引: `phone`, `email`

#### 2.1.3 `t_user_extension` - 用户扩展信息表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 扩展信息ID |
| `user_id` | `BIGINT` | `NOT NULL UNIQUE` | 用户ID，外键关联`t_user.id` |
| `real_name` | `VARCHAR(50)` | `DEFAULT NULL` | 真实姓名 |
| `student_id` | `VARCHAR(50)` | `UNIQUE DEFAULT NULL` | 学号 |
| `id_card` | `VARCHAR(50)` | `DEFAULT NULL` | 身份证号(加密) |
| `id_card_front` | `VARCHAR(255)` | `DEFAULT NULL` | 身份证正面照URL |
| `id_card_back` | `VARCHAR(255)` | `DEFAULT NULL` | 身份证反面照URL |
| `auth_status` | `TINYINT` | `DEFAULT 0` | 实名认证状态(0:未认证,1:审核中,2:已认证,3:认证失败) |
| `auth_remark` | `VARCHAR(255)` | `DEFAULT NULL` | 认证备注 |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引设计**：
- 主键索引: `id`
- 外键索引: `user_id`
- 唯一索引: `student_id`

#### 2.1.4 `t_favorite` - 收藏表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 收藏ID |
| `user_id` | `BIGINT` | `NOT NULL` | 用户ID，外键关联`t_user.id` |
| `product_id` | `BIGINT` | `NOT NULL` | 商品ID，外键关联`t_product.id` |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 收藏时间 |

**索引设计**：
- 主键索引: `id`
- 联合唯一索引: `(user_id, product_id)` - 确保用户不会重复收藏同一商品
- 普通索引: `user_id`, `product_id`

#### 2.1.5 `t_notification_setting` - 消息通知设置表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 设置ID |
| `user_id` | `BIGINT` | `NOT NULL UNIQUE` | 用户ID，外键关联`t_user.id` |
| `message_enabled` | `TINYINT` | `DEFAULT 1` | 新消息提醒(0:关闭,1:开启) |
| `trade_enabled` | `TINYINT` | `DEFAULT 1` | 交易状态变更提醒(0:关闭,1:开启) |
| `system_enabled` | `TINYINT` | `DEFAULT 1` | 系统通知提醒(0:关闭,1:开启) |
| `email_enabled` | `TINYINT` | `DEFAULT 0` | 邮件通知(0:关闭,1:开启) |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引设计**：
- 主键索引: `id`
- 外键索引: `user_id`

### 2.2 商品相关表

#### 2.2.1 `t_product` - 商品表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 商品ID |
| `title` | `VARCHAR(200)` | `NOT NULL` | 商品标题 |
| `description` | `TEXT` | `DEFAULT NULL` | 商品描述 |
| `price` | `DECIMAL(10,2)` | `NOT NULL` | 价格 |
| `original_price` | `DECIMAL(10,2)` | `DEFAULT NULL` | 原价 |
| `user_id` | `BIGINT` | `NOT NULL` | 卖家ID，外键关联`t_user.id` |
| `category_id` | `BIGINT` | `NOT NULL` | 分类ID，外键关联`t_category.id` |
| `status` | `TINYINT` | `DEFAULT 1` | 状态(0:下架,1:在售,2:已售,3:审核中,4:审核失败) |
| `view_count` | `INT` | `DEFAULT 0` | 浏览量 |
| `favorite_count` | `INT` | `DEFAULT 0` | 收藏量 |
| `main_image` | `VARCHAR(255)` | `DEFAULT NULL` | 主图URL |
| `address` | `VARCHAR(255)` | `DEFAULT NULL` | 交易地址 |
| `trade_type` | `TINYINT` | `DEFAULT 1` | 交易方式(1:线下交易,2:快递配送) |
| `delivery_fee` | `DECIMAL(10,2)` | `DEFAULT 0.00` | 快递费用 |
| `reason` | `VARCHAR(255)` | `DEFAULT NULL` | 下架/审核失败原因 |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |
| `last_view_time` | `DATETIME` | `DEFAULT NULL` | 最后浏览时间 |

**索引设计**：
- 主键索引: `id`
- 普通索引: `user_id`, `category_id`, `status`
- 全文索引: `title`, `description` - 用于商品搜索
- 复合索引: `(price, create_time)` - 用于价格排序和时间筛选

#### 2.2.2 `t_product_image` - 商品图片表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 图片ID |
| `product_id` | `BIGINT` | `NOT NULL` | 商品ID，外键关联`t_product.id` |
| `url` | `VARCHAR(255)` | `NOT NULL` | 图片URL |
| `sort` | `INT` | `DEFAULT 0` | 排序顺序 |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |

**索引设计**：
- 主键索引: `id`
- 外键索引: `product_id`

#### 2.2.3 `t_category` - 商品分类表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 分类ID |
| `name` | `VARCHAR(50)` | `NOT NULL` | 分类名称 |
| `parent_id` | `BIGINT` | `DEFAULT 0` | 父分类ID(0表示顶级分类) |
| `icon` | `VARCHAR(255)` | `DEFAULT NULL` | 分类图标URL |
| `sort` | `INT` | `DEFAULT 0` | 排序顺序 |
| `status` | `TINYINT` | `DEFAULT 1` | 状态(0:禁用,1:启用) |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引设计**：
- 主键索引: `id`
- 普通索引: `parent_id`, `status`

#### 2.2.4 `t_product_tag` - 商品标签表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 标签ID |
| `name` | `VARCHAR(50)` | `NOT NULL UNIQUE` | 标签名称 |
| `count` | `INT` | `DEFAULT 0` | 使用次数 |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |

**索引设计**：
- 主键索引: `id`
- 唯一索引: `name`

#### 2.2.5 `t_product_tag_relation` - 商品与标签关系表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 关系ID |
| `product_id` | `BIGINT` | `NOT NULL` | 商品ID，外键关联`t_product.id` |
| `tag_id` | `BIGINT` | `NOT NULL` | 标签ID，外键关联`t_product_tag.id` |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |

**索引设计**：
- 主键索引: `id`
- 联合唯一索引: `(product_id, tag_id)` - 确保商品不会重复关联同一标签
- 普通索引: `product_id`, `tag_id`

### 2.3 订单相关表

#### 2.3.1 `t_order` - 订单表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 订单ID |
| `order_no` | `VARCHAR(50)` | `NOT NULL UNIQUE` | 订单号 |
| `user_id` | `BIGINT` | `NOT NULL` | 买家ID，外键关联`t_user.id` |
| `seller_id` | `BIGINT` | `NOT NULL` | 卖家ID，外键关联`t_user.id` |
| `product_id` | `BIGINT` | `NOT NULL` | 商品ID，外键关联`t_product.id` |
| `total_price` | `DECIMAL(10,2)` | `NOT NULL` | 订单总价 |
| `status` | `TINYINT` | `DEFAULT 0` | 订单状态(0:待付款,1:待发货,2:待收货,3:待评价,4:已完成,5:已取消,6:已退款) |
| `trade_type` | `TINYINT` | `DEFAULT 1` | 交易方式(1:线下交易,2:快递配送) |
| `address` | `VARCHAR(255)` | `DEFAULT NULL` | 收货地址 |
| `contact_phone` | `VARCHAR(20)` | `DEFAULT NULL` | 联系电话 |
| `contact_name` | `VARCHAR(50)` | `DEFAULT NULL` | 联系人姓名 |
| `express_company` | `VARCHAR(50)` | `DEFAULT NULL` | 快递公司 |
| `express_no` | `VARCHAR(50)` | `DEFAULT NULL` | 快递单号 |
| `remark` | `VARCHAR(255)` | `DEFAULT NULL` | 订单备注 |
| `cancel_reason` | `VARCHAR(255)` | `DEFAULT NULL` | 取消原因 |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |
| `pay_time` | `DATETIME` | `DEFAULT NULL` | 支付时间 |
| `delivery_time` | `DATETIME` | `DEFAULT NULL` | 发货时间 |
| `confirm_time` | `DATETIME` | `DEFAULT NULL` | 确认收货时间 |
| `cancel_time` | `DATETIME` | `DEFAULT NULL` | 取消时间 |

**索引设计**：
- 主键索引: `id`
- 唯一索引: `order_no`
- 复合索引: `(user_id, status)` - 用于查询用户订单列表
- 复合索引: `(seller_id, status)` - 用于查询卖家订单列表
- 普通索引: `product_id`, `status`

#### 2.3.2 `t_order_item` - 订单项表（支持多商品订单扩展）

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 订单项ID |
| `order_id` | `BIGINT` | `NOT NULL` | 订单ID，外键关联`t_order.id` |
| `product_id` | `BIGINT` | `NOT NULL` | 商品ID，外键关联`t_product.id` |
| `product_name` | `VARCHAR(200)` | `NOT NULL` | 商品名称(快照) |
| `product_image` | `VARCHAR(255)` | `DEFAULT NULL` | 商品图片(快照) |
| `price` | `DECIMAL(10,2)` | `NOT NULL` | 商品单价 |
| `quantity` | `INT` | `NOT NULL DEFAULT 1` | 商品数量 |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |

**索引设计**：
- 主键索引: `id`
- 外键索引: `order_id`, `product_id`

#### 2.3.3 `t_review` - 评价表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 评价ID |
| `order_id` | `BIGINT` | `NOT NULL UNIQUE` | 订单ID，外键关联`t_order.id` |
| `product_id` | `BIGINT` | `NOT NULL` | 商品ID，外键关联`t_product.id` |
| `user_id` | `BIGINT` | `NOT NULL` | 评价用户ID，外键关联`t_user.id` |
| `target_user_id` | `BIGINT` | `NOT NULL` | 被评价用户ID，外键关联`t_user.id` |
| `rating` | `TINYINT` | `NOT NULL` | 评分(1-5星) |
| `content` | `TEXT` | `DEFAULT NULL` | 评价内容 |
| `images` | `VARCHAR(500)` | `DEFAULT NULL` | 评价图片URL，JSON格式 |
| `reply_content` | `TEXT` | `DEFAULT NULL` | 回复内容 |
| `reply_time` | `DATETIME` | `DEFAULT NULL` | 回复时间 |
| `status` | `TINYINT` | `DEFAULT 1` | 状态(0:隐藏,1:显示) |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引设计**：
- 主键索引: `id`
- 唯一索引: `order_id`
- 复合索引: `(product_id, rating)` - 用于商品评价查询和排序
- 普通索引: `user_id`, `target_user_id`, `status`

### 2.4 聊天相关表

#### 2.4.1 `t_chat_session` - 聊天会话表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 会话ID |
| `user_id1` | `BIGINT` | `NOT NULL` | 用户1 ID，外键关联`t_user.id` |
| `user_id2` | `BIGINT` | `NOT NULL` | 用户2 ID，外键关联`t_user.id` |
| `last_message` | `VARCHAR(500)` | `DEFAULT NULL` | 最后一条消息内容 |
| `last_time` | `DATETIME` | `DEFAULT NULL` | 最后一条消息时间 |
| `user1_unread` | `INT` | `DEFAULT 0` | 用户1未读数 |
| `user2_unread` | `INT` | `DEFAULT 0` | 用户2未读数 |
| `status` | `TINYINT` | `DEFAULT 1` | 状态(0:关闭,1:正常) |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引设计**：
- 主键索引: `id`
- 联合唯一索引: `(user_id1, user_id2)` - 确保两个用户之间只有一个会话
- 复合索引: `(user_id1, last_time)` - 用于查询用户会话列表并按时间排序
- 复合索引: `(user_id2, last_time)` - 用于查询用户会话列表并按时间排序

#### 2.4.2 `t_message` - 消息表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 消息ID |
| `session_id` | `BIGINT` | `NOT NULL` | 会话ID，外键关联`t_chat_session.id` |
| `sender_id` | `BIGINT` | `NOT NULL` | 发送者ID，外键关联`t_user.id` |
| `receiver_id` | `BIGINT` | `NOT NULL` | 接收者ID，外键关联`t_user.id` |
| `content` | `TEXT` | `DEFAULT NULL` | 消息内容 |
| `type` | `TINYINT` | `DEFAULT 1` | 消息类型(1:文本,2:图片,3:语音,4:文件,5:系统消息) |
| `status` | `TINYINT` | `DEFAULT 0` | 消息状态(0:未读,1:已读) |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |

**索引设计**：
- 主键索引: `id`
- 复合索引: `(session_id, create_time)` - 用于查询会话消息历史
- 复合索引: `(receiver_id, status, create_time)` - 用于查询未读消息
- 普通索引: `sender_id`

### 2.5 搜索相关表

#### 2.5.1 `t_search_history` - 搜索历史表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 历史记录ID |
| `user_id` | `BIGINT` | `DEFAULT NULL` | 用户ID，外键关联`t_user.id`，NULL表示匿名搜索 |
| `keyword` | `VARCHAR(100)` | `NOT NULL` | 搜索关键词 |
| `search_count` | `INT` | `DEFAULT 1` | 搜索次数 |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 第一次搜索时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 最后搜索时间 |

**索引设计**：
- 主键索引: `id`
- 复合索引: `(user_id, keyword)` - 用于用户搜索历史查询
- 普通索引: `keyword`, `update_time`

#### 2.5.2 `t_hot_word` - 热门搜索词表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 热门词ID |
| `keyword` | `VARCHAR(100)` | `NOT NULL UNIQUE` | 关键词 |
| `count` | `INT` | `DEFAULT 0` | 搜索次数 |
| `sort` | `INT` | `DEFAULT 0` | 排序权重 |
| `status` | `TINYINT` | `DEFAULT 1` | 状态(0:禁用,1:启用) |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `update_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引设计**：
- 主键索引: `id`
- 唯一索引: `keyword`
- 复合索引: `(status, count, sort)` - 用于热门词排序查询

### 2.6 系统相关表

#### 2.6.1 `t_notification` - 通知表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 通知ID |
| `user_id` | `BIGINT` | `NOT NULL` | 接收用户ID，外键关联`t_user.id` |
| `type` | `VARCHAR(50)` | `NOT NULL` | 通知类型 |
| `title` | `VARCHAR(200)` | `DEFAULT NULL` | 通知标题 |
| `content` | `TEXT` | `NOT NULL` | 通知内容 |
| `link` | `VARCHAR(255)` | `DEFAULT NULL` | 跳转链接 |
| `is_read` | `TINYINT` | `DEFAULT 0` | 是否已读(0:未读,1:已读) |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |
| `read_time` | `DATETIME` | `DEFAULT NULL` | 阅读时间 |

**索引设计**：
- 主键索引: `id`
- 复合索引: `(user_id, is_read)` - 用于查询用户通知
- 普通索引: `type`, `create_time`

#### 2.6.2 `t_file` - 文件表

| 字段名 | 数据类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | `BIGINT` | `PRIMARY KEY AUTO_INCREMENT` | 文件ID |
| `file_name` | `VARCHAR(255)` | `NOT NULL` | 文件名 |
| `file_path` | `VARCHAR(255)` | `NOT NULL` | 文件存储路径 |
| `file_size` | `BIGINT` | `DEFAULT 0` | 文件大小(字节) |
| `file_type` | `VARCHAR(50)` | `DEFAULT NULL` | 文件类型 |
| `md5` | `VARCHAR(32)` | `DEFAULT NULL` | 文件MD5值 |
| `upload_user_id` | `BIGINT` | `DEFAULT NULL` | 上传用户ID |
| `related_id` | `BIGINT` | `DEFAULT NULL` | 关联ID |
| `related_type` | `VARCHAR(50)` | `DEFAULT NULL` | 关联类型 |
| `status` | `TINYINT` | `DEFAULT 1` | 状态(0:删除,1:正常) |
| `create_time` | `DATETIME` | `NOT NULL DEFAULT CURRENT_TIMESTAMP` | 创建时间 |

**索引设计**：
- 主键索引: `id`
- 唯一索引: `md5` - 用于文件去重
- 复合索引: `(related_type, related_id)` - 用于关联查询

## 3. 数据库关系图

```
+----------------+       +----------------+       +-------------------+
|    t_user      |<------|   t_user_auth  |       |t_user_extension   |
+----------------+       +----------------+       +-------------------+
| id             |       | id             |       | id                |
| username       |       | user_id        |       | user_id           |
| nickname       |       | password       |       | real_name         |
| avatar         |       | phone          |       | student_id        |
| gender         |       | email          |       | id_card           |
| college        |       | login_attempts |       +-------------------+
| grade          |       +----------------+               |
| status         |                                       |
+------+---------+                                       |
       |                                                 |
       |                                                 |
+------v---------+       +----------------+       +------v-------------+
|  t_notification_setting |       |  t_favorite    |       |t_notification     |
+----------------+       +----------------+       +-------------------+
| id             |       | id             |       | id                |
| user_id        |       | user_id        |       | user_id           |
| message_enabled|       | product_id     |       | type              |
| trade_enabled  |       +----------------+       | title             |
| system_enabled |                               | content           |
+----------------+                               +-------------------+

+----------------+       +----------------+       +-------------------+
|    t_product   |<------|t_product_image |       |     t_category    |
+----------------+       +----------------+       +-------------------+
| id             |       | id             |       | id                |
| title          |       | product_id     |       | name              |
| description    |       | url            |       | parent_id         |
| price          |       | sort           |       | icon              |
| user_id        |       +----------------+       | sort              |
| category_id    |                               | status            |
| status         |                               +-------------------+
| view_count     |                                       |
| favorite_count |                                       |
+------+---------+                                       |
       |                                                 |
       |                                                 |
+------v---------+       +----------------+       +------v-------------+
| t_product_tag_relation|       |  t_product_tag |       |     t_review      |
+----------------+       +----------------+       +-------------------+
| id             |       | id             |       | id                |
| product_id     |       | name           |       | order_id          |
| tag_id         |       | count          |       | product_id        |
+----------------+       +----------------+       | user_id           |
                                                   | target_user_id    |
+----------------+       +----------------+       | rating            |
|      t_order   |<------|  t_order_item  |       | content           |
+----------------+       +----------------+       +-------------------+
| id             |       | id             |
| order_no       |       | order_id       |
| user_id        |       | product_id     |
| seller_id      |       | product_name   |
| product_id     |       | price          |
| total_price    |       | quantity       |
| status         |       +----------------+       +-------------------+
| trade_type     |                                 |    t_message      |
+----------------+                                 +-------------------+
                                                   | id                |
+----------------+       +----------------+       | session_id        |
|  t_chat_session|<------|    t_message   |       | sender_id         |
+----------------+       +----------------+       | receiver_id       |
| id             |       | id             |       | content           |
| user_id1       |       | session_id     |       | type              |
| user_id2       |       | sender_id      |       | status            |
| last_message   |       | receiver_id    |       +-------------------+
| last_time      |       | content        |
| user1_unread   |       | type           |       +-------------------+
| user2_unread   |       +----------------+       |   t_search_history|
+----------------+                                 +-------------------+
                                                   | id                |
+----------------+       +----------------+       | user_id           |
|    t_hot_word  |       |   t_file       |       | keyword           |
+----------------+       +----------------+       | search_count      |
| id             |       | id             |       +-------------------+
| keyword        |       | file_name      |
| count          |       | file_path      |
| sort           |       | file_size      |
| status         |       | upload_user_id |
+----------------+       | related_id     |
                         | related_type   |
                         +----------------+
```

## 4. 数据迁移与初始化方案

### 4.1 数据库初始化脚本

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS secondhand CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 切换到secondhand数据库
USE secondhand;

-- 创建用户表
CREATE TABLE IF NOT EXISTS t_user (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  username VARCHAR(50) NOT NULL UNIQUE,
  nickname VARCHAR(50) NOT NULL,
  avatar VARCHAR(255) DEFAULT NULL,
  gender TINYINT DEFAULT 0,
  college VARCHAR(100) DEFAULT NULL,
  grade INT DEFAULT NULL,
  status TINYINT DEFAULT 1,
  last_login_time DATETIME DEFAULT NULL,
  create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 创建索引
CREATE INDEX idx_user_status ON t_user(status);

-- 其他表创建语句类似...

-- 初始化基础数据
-- 1. 初始化商品分类
INSERT INTO t_category (name, parent_id, icon, sort, status) VALUES
('电子产品', 0, 'electronics.png', 1, 1),
('手机数码', 1, 'phone.png', 1, 1),
('电脑办公', 1, 'computer.png', 2, 1),
('家用电器', 0, 'appliance.png', 2, 1),
('图书文具', 0, 'book.png', 3, 1),
('服装鞋包', 0, 'clothes.png', 4, 1),
('生活用品', 0, 'daily.png', 5, 1),
('运动健身', 0, 'sports.png', 6, 1),
('其他物品', 0, 'other.png', 7, 1);

-- 2. 初始化管理员账户
INSERT INTO t_user (username, nickname, avatar, status) VALUES
('admin', '系统管理员', 'admin.png', 1);

INSERT INTO t_user_auth (user_id, password, phone, email) VALUES
(1, '$2a$10$Q8Z1j5zJ8qR6xU6H2L3WZ.4X4X4X4X4X4X4X4X4X4X4X4X4X4X4', '13800138000', 'admin@secondhand.com');
```

### 4.2 数据迁移策略

- **版本控制**：使用 Flyway 或 Liquibase 进行数据库版本管理
- **增量更新**：每次更新只修改必要的表结构和数据
- **备份机制**：重要数据迁移前进行数据库备份
- **回滚方案**：提供数据迁移失败的回滚脚本

## 5. 数据库优化策略

### 5.1 索引优化
- 为频繁查询的字段创建索引
- 为外键字段创建索引
- 避免在经常更新的字段上创建索引
- 定期分析和优化索引

### 5.2 查询优化
- 使用 JOIN 代替子查询
- 避免使用 SELECT *，只查询必要字段
- 使用 LIMIT 限制返回结果数量
- 合理使用 WHERE 条件，避免全表扫描
- 使用预处理语句防止 SQL 注入

### 5.3 分区表
- 对于历史订单、聊天记录等大数据量表，考虑使用分区表
- 按时间范围或业务类型进行分区
- 提高数据查询效率和管理便捷性

### 5.4 读写分离
- 采用主从复制架构
- 主库负责写操作，从库负责读操作
- 提高系统并发性能

### 5.5 缓存策略
- 使用 Redis 缓存热点数据
- 缓存商品详情、分类信息等不经常变动的数据
- 设置合理的缓存过期时间

## 6. 数据库安全策略

### 6.1 密码安全
- 密码使用 BCrypt 加密存储
- 定期提醒用户修改密码
- 限制登录失败次数，防止暴力破解

### 6.2 数据加密
- 敏感数据（如身份证号）进行加密存储
- 传输过程中使用 HTTPS 加密

### 6.3 权限控制
- 使用最小权限原则配置数据库用户
- 不同服务使用不同的数据库账号
- 禁止使用 root 用户连接应用

### 6.4 SQL 注入防护
- 使用参数化查询
- 对用户输入进行严格验证
- 限制 SQL 语句长度

### 6.5 数据备份与恢复
- 定期进行全量备份和增量备份
- 异地备份重要数据
- 定期测试备份数据的恢复流程

## 7. 扩展性设计

### 7.1 分库分表预留
- 设计时考虑未来可能的数据量增长
- 预留分库分表的字段和主键设计
- 考虑使用分布式数据库中间件

### 7.2 冷热数据分离
- 活跃数据保留在主表
- 历史数据迁移到历史表或归档表
- 提高查询效率，降低存储成本

### 7.3 读写分离扩展
- 支持多从库配置
- 支持动态添加从库
- 实现读写分离中间件
# 校园二手物品交易平台概要设计文档

## 1. 系统概述

### 1.1 系统目标
设计并实现一个基于微服务架构的校园二手物品交易平台，提供用户管理、商品交易、订单管理、实时聊天和搜索等功能，确保系统高性能、高可用、安全可靠，并支持未来的扩展。

### 1.2 设计原则
- **微服务架构**：将系统拆分为独立的微服务，实现服务间的松耦合和高内聚
- **高可用性**：通过服务冗余、集群部署、故障转移等机制确保系统稳定运行
- **可扩展性**：支持水平扩展，以应对不断增长的用户量和业务需求
- **安全性**：采用多层次的安全防护措施，保障用户数据和交易安全
- **性能优先**：优化系统性能，提供快速的响应体验

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         前端层 (Vue3)                            │
└─────────────────────────────────────┬─────────────────────────────┘
                                      │ HTTPS
┌─────────────────────────────────────▼─────────────────────────────┐
│                     API Gateway (Spring Cloud Gateway)             │
└───────────────────┬─────────────────┬─────────────────┬───────────┘
                    │                 │                 │
┌───────────────────▼─┐ ┌─────────────▼─────┐ ┌─────────▼───────────┐
│     用户服务        │ │    商品服务       │ │    订单服务         │
│  (User-Service)    │ │ (Product-Service)  │ │  (Order-Service)    │
└─────────┬───────────┘ └─────────┬─────────┘ └─────────┬───────────┘
          │                       │                       │
┌─────────▼───────────┐ ┌─────────▼─────────┐ ┌─────────▼───────────┐
│     聊天服务        │ │    搜索服务       │ │   配置中心(Nacos)    │
│  (Chat-Service)    │ │ (Search-Service)  │ └─────────┬───────────┘
└─────────┬───────────┘ └─────────┬─────────┘          │
          │                       │                    │
┌─────────▼───────────────────────▼────────────────────▼───────────┐
│            服务治理 (Sentinel + Seata + OpenFeign)                │
└─────────┬───────────────────────┬────────────────────────────────┘
          │                       │
┌─────────▼─────────┐     ┌─────────────────────────────────┐
│    MySQL数据库    │     │         Redis缓存               │
└───────────────────┘     └─────────────────────────────────┘
```

### 2.2 分层架构

#### 2.2.1 表现层
- **前端技术栈**：Vue3 + Vite + Vue Router + Pinia + Element Plus
- **职责**：用户界面展示、用户交互处理、数据展示
- **特点**：响应式设计、组件化开发、支持移动端适配

#### 2.2.2 网关层
- **技术栈**：Spring Cloud Gateway
- **职责**：请求路由、负载均衡、统一认证、限流控制、跨域处理

#### 2.2.3 业务层
- **技术栈**：Spring Boot + Spring Cloud + Spring Cloud Alibaba
- **服务列表**：
  - 用户服务 (User-Service)
  - 商品服务 (Product-Service)
  - 订单服务 (Order-Service)
  - 聊天服务 (Chat-Service)
  - 搜索服务 (Search-Service)
- **职责**：业务逻辑处理、数据处理

#### 2.2.4 数据访问层
- **技术栈**：MyBatis-Plus (注解 + XML) + MySQL
- **职责**：数据持久化、数据查询

#### 2.2.5 基础设施层
- **配置中心**：Nacos
- **服务注册发现**：Nacos
- **熔断降级**：Sentinel
- **分布式事务**：Seata
- **缓存**：Redis
- **消息队列**：RabbitMQ (用于异步处理)
- **搜索引擎**：Elasticsearch (支持搜索服务)

## 3. 微服务设计

### 3.1 用户服务 (User-Service)

#### 3.1.1 功能概述
负责用户注册、登录、个人信息管理、收藏夹管理和消息通知设置等功能。

#### 3.1.2 核心接口
- 用户注册：`/api/v1/users/register`
- 用户登录：`/api/v1/users/login`
- 获取用户信息：`/api/v1/users/info`
- 更新用户信息：`/api/v1/users/update`
- 收藏商品：`/api/v1/users/favorites/add`
- 取消收藏：`/api/v1/users/favorites/remove`
- 获取收藏列表：`/api/v1/users/favorites`

#### 3.1.3 数据模型
- `User`：用户基本信息
- `UserAuth`：用户认证信息
- `Favorite`：用户收藏记录
- `NotificationSetting`：消息通知设置

### 3.2 商品服务 (Product-Service)

#### 3.2.1 功能概述
负责商品的发布、编辑、删除、搜索、筛选、状态管理和分类管理等功能。

#### 3.2.2 核心接口
- 发布商品：`/api/v1/products` (POST)
- 编辑商品：`/api/v1/products/{id}` (PUT)
- 删除商品：`/api/v1/products/{id}` (DELETE)
- 获取商品详情：`/api/v1/products/{id}` (GET)
- 获取商品列表：`/api/v1/products` (GET)
- 商品分类列表：`/api/v1/categories` (GET)
- 商品状态更新：`/api/v1/products/{id}/status` (PUT)

#### 3.2.3 数据模型
- `Product`：商品基本信息
- `ProductImage`：商品图片
- `Category`：商品分类
- `ProductTag`：商品标签

### 3.3 订单服务 (Order-Service)

#### 3.3.1 功能概述
负责订单的创建、管理、状态跟踪和评价系统等功能。

#### 3.3.2 核心接口
- 创建订单：`/api/v1/orders` (POST)
- 获取订单列表：`/api/v1/orders` (GET)
- 获取订单详情：`/api/v1/orders/{id}` (GET)
- 更新订单状态：`/api/v1/orders/{id}/status` (PUT)
- 创建评价：`/api/v1/orders/{id}/review` (POST)
- 获取评价列表：`/api/v1/products/{id}/reviews` (GET)

#### 3.3.3 数据模型
- `Order`：订单基本信息
- `OrderItem`：订单项
- `Review`：评价信息

### 3.4 聊天服务 (Chat-Service)

#### 3.4.1 功能概述
负责实时消息通信和聊天记录存储等功能。

#### 3.4.2 核心接口
- WebSocket连接：`/ws/chat/{userId}`
- 获取会话列表：`/api/v1/chat/sessions` (GET)
- 获取聊天记录：`/api/v1/chat/history/{sessionId}` (GET)

#### 3.4.3 数据模型
- `ChatSession`：聊天会话
- `Message`：消息记录

### 3.5 搜索服务 (Search-Service)

#### 3.5.1 功能概述
负责商品模糊搜索、热门搜索词和搜索历史等功能。

#### 3.5.2 核心接口
- 商品搜索：`/api/v1/search/products` (GET)
- 热门搜索词：`/api/v1/search/hotwords` (GET)
- 获取搜索历史：`/api/v1/search/history` (GET)
- 清除搜索历史：`/api/v1/search/history/clear` (DELETE)

#### 3.5.3 数据模型
- `SearchHistory`：搜索历史
- `HotWord`：热门搜索词

## 4. 技术选型

### 4.1 前端技术
- **框架**：Vue 3
- **构建工具**：Vite
- **路由**：Vue Router
- **状态管理**：Pinia
- **UI组件库**：Element Plus
- **HTTP客户端**：Axios
- **Websocket客户端**：原生WebSocket API
- **样式预处理器**：SCSS

### 4.2 后端技术
- **基础框架**：Spring Boot 2.7.x
- **微服务框架**：Spring Cloud 2021.x
- **服务注册发现**：Nacos 2.0.x
- **API网关**：Spring Cloud Gateway
- **服务调用**：OpenFeign
- **熔断降级**：Sentinel
- **分布式事务**：Seata
- **ORM框架**：MyBatis-Plus
- **数据库**：MySQL 8.0
- **缓存**：Redis 6.x
- **消息队列**：RabbitMQ
- **搜索引擎**：Elasticsearch 7.x
- **WebSocket**：Spring WebSocket

### 4.3 运维技术
- **容器化**：Docker
- **容器编排**：Docker Compose
- **CI/CD**：Jenkins
- **监控**：Prometheus + Grafana
- **日志管理**：ELK Stack

## 5. 关键设计

### 5.1 安全设计
- **认证机制**：JWT (JSON Web Token)
- **授权机制**：基于角色的访问控制 (RBAC)
- **数据加密**：HTTPS + AES加密
- **密码加密**：BCrypt
- **防攻击措施**：SQL注入防护、XSS防护、CSRF防护、接口限流

### 5.2 性能优化设计
- **缓存策略**：多级缓存 (本地缓存 + Redis)
- **数据库优化**：索引优化、查询优化、分库分表预留
- **异步处理**：消息队列处理耗时操作
- **服务降级**：非核心功能降级保护核心功能
- **CDN加速**：静态资源CDN加速

### 5.3 高可用设计
- **服务冗余**：核心服务多实例部署
- **负载均衡**：Ribbon + Nginx
- **熔断降级**：Sentinel保障服务可用性
- **服务限流**：防止流量突增导致系统崩溃
- **数据备份**：定期数据库备份和恢复机制

## 6. 部署架构

### 6.1 开发环境
- 本地开发环境：Docker Compose编排所有服务
- 测试环境：专用测试服务器，模拟生产环境配置

### 6.2 生产环境
- **Web服务器**：Nginx (静态资源 + API网关前置)
- **应用服务器**：多台云服务器集群
- **数据库服务器**：主从架构
- **缓存服务器**：Redis集群
- **消息队列服务器**：RabbitMQ集群
- **搜索引擎服务器**：Elasticsearch集群

### 6.3 网络架构
- 外网 -> CDN -> Nginx -> API Gateway -> 微服务集群 -> 数据库/缓存/消息队列
- 内部服务通信：内网通信，避免外网暴露

## 7. 开发规范

### 7.1 代码规范
- 遵循Java编码规范 (Google Java Style Guide)
- 遵循前端编码规范 (Airbnb JavaScript Style Guide)
- 代码提交前必须通过代码审查和静态代码分析

### 7.2 接口规范
- RESTful API设计规范
- 统一的接口响应格式
- 统一的错误码和错误信息

### 7.3 数据库规范
- 表命名规范：`t_模块名_表名`
- 字段命名规范：`小驼峰命名法`
- 索引规范：合理创建索引，避免过度索引

### 7.4 文档规范
- API文档：Swagger/OpenAPI
- 代码注释：Javadoc (Java)、JSDoc (JavaScript)
- 架构文档：定期更新架构设计文档